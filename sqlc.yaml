version: 2
sql:
  - schema: "sql/fixtures.sql"
    queries: "sql/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "go/gen/db"
        emit_json_tags: true
        emit_pointers_for_null_types: true